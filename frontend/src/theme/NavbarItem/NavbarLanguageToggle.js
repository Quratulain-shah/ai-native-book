import React from 'react';
import LanguageToggle from '../../components/LanguageToggle';

// This is the custom Navbar item component
export default function NavbarLanguageToggle(props) {
  return <LanguageToggle {...props} />;
}